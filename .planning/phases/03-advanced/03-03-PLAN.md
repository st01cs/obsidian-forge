---
phase: 03-advanced
plan: '03'
type: execute
wave: '2'
depends_on:
  - 03-01
  - 03-02
files_modified:
  - src/commands/weekly.ts
  - src/commands/1on1.ts
  - src/commands/incident.ts
  - src/commands/brag.ts
  - src/commands/report.ts
  - src/commands/audit.ts
  - src/main.ts
autonomous: true
requirements:
  - CMND-04
  - CMND-05
  - CMND-06
  - CMND-07
  - CMND-08
  - CMND-09

must_haves:
  truths:
    - "/weekly command produces cross-session weekly summary with pattern discovery and 成果 report"
    - "/1on1 command structures meeting notes into standard 1:1 format"
    - "/incident command captures incident from Slack, reconstructs timeline, creates event doc"
    - "/brag command records 成果 with evidence links to Brag Doc"
    - "/report command generates performance review brief from evidence chain"
    - "/audit command checks knowledge base: orphans, broken links, frontmatter gaps, stale content"
  artifacts:
    - path: src/commands/weekly.ts
      provides: "CMND-04: /weekly command implementation"
      min_lines: 80
    - path: src/commands/1on1.ts
      provides: "CMND-05: /1on1 command implementation"
      min_lines: 70
    - path: src/commands/incident.ts
      provides: "CMND-06: /incident command using Slack integration"
      min_lines: 90
    - path: src/commands/brag.ts
      provides: "CMND-07: /brag command using GitHub integration"
      min_lines: 80
    - path: src/commands/report.ts
      provides: "CMND-08: /report command for performance review brief"
      min_lines: 80
    - path: src/commands/audit.ts
      provides: "CMND-09: /audit command for knowledge base checks"
      min_lines: 90
  key_links:
    - from: src/commands/incident.ts
      to: src/integrations/slack-client.ts
      via: Uses SlackClient for incident reconstruction
      pattern: "SlackClient"
    - from: src/commands/brag.ts
      to: src/integrations/github-client.ts
      via: Uses GitHubClient for evidence links
      pattern: "GitHubClient"
    - from: src/commands/audit.ts
      to: src/agents/sub-agent-manager.ts
      via: Uses SubAgentManager.runSubAgentWithIdleGuard() for heavy audit
      pattern: "runSubAgentWithIdleGuard"
---

<objective>
Implement all six advanced commands: /weekly, /1on1, /incident, /brag, /report, /audit. All follow session.prompt() pattern from Phase 2. Some commands use sub-agents and external integrations from Plans 01 and 02.
</objective>

<context>
@src/AgentBridge.ts (session.prompt() pattern)
@src/commands/standup.ts (existing command pattern to follow)
@src/commands/review.ts (existing command pattern to follow)
@src/integrations/slack-client.ts (Plan 02 - SlackClient)
@src/integrations/github-client.ts (Plan 02 - GitHubClient)
@src/agents/sub-agent-manager.ts (Plan 01 - SubAgentManager)
@.planning/phases/03-advanced/03-RESEARCH.md (command implementation patterns)
@src/main.ts (settings interface for tokens)

## Key Patterns

All commands follow the Phase 2 `session.prompt()` pattern established in standup.ts, free-dump.ts, review.ts.

**CMND-06 (/incident):** Uses SlackClient to fetch incident messages, reconstructs timeline, creates event doc.

**CMND-07 (/brag):** Uses GitHubClient to get PR details as evidence, records to performance/brag.md.

**CMND-09 (/audit):** Uses SubAgentManager.runSubAgentWithIdleGuard() for non-blocking heavy audit.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create weekly.ts command (CMND-04)</name>
  <files>src/commands/weekly.ts</files>
  <action>
Create `src/commands/weekly.ts` - /weekly command implementation.

**CMND-04:** Cross-session weekly summary, pattern discovery, 成果 report.

Implement `executeWeeklyCommand(agentBridge: AgentBridge)` that:
1. Checks if agent is initialized
2. Shows "Compiling weekly summary..." notice
3. Calls session.prompt() with a prompt that instructs the agent to:
   - Analyze cognitive/sessions/ notes from past week
   - Look for patterns: recurring themes, decisions, progress, blockers, wins
   - Scan performance/ and work/ notes for achievements
   - Update index files
   - Output to forge/cognitive/sessions/YYYY-WXX.md (ISO week format)

Also export `getISOWeekNumber(date: Date): string` helper.
</action>
  <verify>
<automated>grep -c "executeWeeklyCommand\|session\\.prompt" src/commands/weekly.ts && grep -c "ISO week\|Pattern Discovery" src/commands/weekly.ts</automated>
  <done>weekly.ts created with executeWeeklyCommand() per CMND-04</done>
</task>

<task type="auto">
  <name>Task 2: Create 1on1.ts command (CMND-05)</name>
  <files>src/commands/1on1.ts</files>
  <action>
Create `src/commands/1on1.ts` - /1on1 command implementation.

**CMND-05:** Structure meeting notes into standard 1:1 format.

Implement `execute1on1Command(agentBridge: AgentBridge)` that:
1. Checks if agent is initialized
2. Shows "1:1 formatting mode..." notice
3. Calls session.prompt() with a prompt that instructs the agent to:
   - Ask user for meeting notes or description
   - Extract participant name, discussion topics, decisions, action items
   - Format into standard 1:1 structure
   - Save to work/meetings/YYYY-MM-DD-1on1-[participant].md

Also export `format1on1Notes(data)` helper function for structured formatting.
</action>
  <verify>
<automated>grep -c "execute1on1Command\|session\\.prompt" src/commands/1on1.ts && grep -c "1:1\\|participant" src/commands/1on1.ts</automated>
  <done>1on1.ts created with execute1on1Command() per CMND-05</done>
</task>

<task type="auto">
  <name>Task 3: Create incident.ts command (CMND-06)</name>
  <files>src/commands/incident.ts</files>
  <action>
Create `src/commands/incident.ts` - /incident command using Slack integration.

**CMND-06:** Capture incident from Slack, reconstruct timeline, create event doc.

Implement `executeIncidentCommand(agentBridge: AgentBridge)` that:
1. Checks if agent is initialized
2. Checks for slackToken in settings
3. Shows "Incident capture mode..." notice
4. Creates SlackClient instance with app and token
5. Calls session.prompt() with a prompt instructing agent to:
   - Ask for Slack channel/thread URL or message IDs
   - Use http_request tool to fetch Slack messages
   - Parse messages chronologically
   - Create event doc at reference/events/YYYY-MM-DD-incident-[slug].md

Also export `fetchIncidentTimeline(slackClient, channelId, startTime, endTime)` helper.
</action>
  <verify>
<automated>grep -c "executeIncidentCommand\|SlackClient\|session\\.prompt" src/commands/incident.ts && grep -c "reference/events\\|incident" src/commands/incident.ts</automated>
  <done>incident.ts created with executeIncidentCommand() per CMND-06</done>
</task>

<task type="auto">
  <name>Task 4: Create brag.ts command (CMND-07)</name>
  <files>src/commands/brag.ts</files>
  <action>
Create `src/commands/brag.ts` - /brag command using GitHub integration.

**CMND-07:** Record 成果 with evidence links to Brag Doc.

Implement `executeBragCommand(agentBridge: AgentBridge)` that:
1. Checks if agent is initialized
2. Checks for githubToken in settings
3. Shows "Brag mode!" notice
4. Creates GitHubClient instance
5. Calls session.prompt() with a prompt instructing agent to:
   - Ask user what they achieved, when, impact
   - Use http_request to fetch PR details if mentioned
   - Update performance/brag.md with new entry

Also export `formatBragEntry(data)` helper function.
</action>
  <verify>
<automated>grep -c "executeBragCommand\|GitHubClient\|session\\.prompt" src/commands/brag.ts && grep -c "performance/brag\\|achievement" src/commands/brag.ts</automated>
  <done>brag.ts created with executeBragCommand() per CMND-07</done>
</task>

<task type="auto">
  <name>Task 5: Create report.ts command (CMND-08)</name>
  <files>src/commands/report.ts</files>
  <action>
Create `src/commands/report.ts` - /report command for performance review brief.

**CMND-08:** Generate performance review brief from evidence chain.

Implement `executeReportCommand(agentBridge: AgentBridge)` that:
1. Checks if agent is initialized
2. Shows "Generating performance brief..." notice
3. Calls session.prompt() with a prompt instructing agent to:
   - Scan performance/brag.md for entries in review period
   - Scan work/ for project completions
   - Check cognitive/decisions/ for key decisions
   - Review session summaries for context
   - Generate structured brief
   - Write to performance/reviews/YYYY-MM-DD-review-brief.md

Also export `getReviewPeriod(lastReviewDate?)` helper.
</action>
  <verify>
<automated>grep -c "executeReportCommand\|session\\.prompt" src/commands/report.ts && grep -c "performance-review\\|achievements" src/commands/report.ts</automated>
  <done>report.ts created with executeReportCommand() per CMND-08</done>
</task>

<task type="auto">
  <name>Task 6: Create audit.ts command (CMND-09)</name>
  <files>src/commands/audit.ts</files>
  <action>
Create `src/commands/audit.ts` - /audit command for knowledge base checks.

**CMND-09:** Check knowledge base: orphans, broken links, frontmatter gaps, stale content.

Implement `executeAuditCommand(agentBridge: AgentBridge)` that:
1. Checks if agent is initialized
2. Shows "Starting knowledge base audit..." notice
3. Calls session.prompt() with audit checklist prompt
4. If SubAgentManager available, also runs audit-agent via runSubAgentWithIdleGuard() for background deep audit
5. Writes final report to cognitive/audits/YYYY-MM-DD-audit.md

The prompt should cover:
- Orphan notes check (get_orphans tool)
- Frontmatter completeness (date, description, tags)
- Stale content (90+ days old)
- Long notes without wikilinks
- Index file status

Also export `runQuickAudit(agentBridge)` helper for non-subagent fallback.
</action>
  <verify>
<automated>grep -c "executeAuditCommand\|session\\.prompt\|runSubAgentWithIdleGuard" src/commands/audit.ts && grep -c "get_orphans\\|frontmatter\\|stale" src/commands/audit.ts</automated>
  <done>audit.ts created with executeAuditCommand() per CMND-09</done>
</task>

<task type="auto">
  <name>Task 7: Wire all commands to main.ts</name>
  <files>src/main.ts</files>
  <action>
Update `src/main.ts` to wire all 6 new commands to actual implementations.

**Add imports:**
```typescript
import { executeWeeklyCommand } from './commands/weekly';
import { execute1on1Command } from './commands/1on1';
import { executeIncidentCommand } from './commands/incident';
import { executeBragCommand } from './commands/brag';
import { executeReportCommand } from './commands/report';
import { executeAuditCommand } from './commands/audit';
```

**Update registerCommands()** - replace Phase 3 stub loop with actual implementations:
- forge-weekly: calls executeWeeklyCommand
- forge-1on1: calls execute1on1Command
- forge-incident: calls executeIncidentCommand
- forge-brag: calls executeBragCommand
- forge-report: calls executeReportCommand
- forge-audit: calls executeAuditCommand

Each command should:
1. Open chat panel (getLeaf false, setViewState VIEW_TYPE_CHAT)
2. Check agentBridge exists and call the command function
3. Show Notice if agent not initialized

**Remove the Phase 3+ stub loop** that currently handles weekly, 1on1, incident, brag, report, audit as stubs.
</action>
  <verify>
<automated>grep -c "executeWeeklyCommand\|execute1on1Command\|executeIncidentCommand\|executeBragCommand\|executeReportCommand\|executeAuditCommand" src/main.ts</automated>
  <done>main.ts updated with all 6 command implementations wired per CMND-04 through CMND-09</done>
</task>

</tasks>

<verification>
## Phase 3 Plan 03 Verification

1. `src/commands/weekly.ts` exists with executeWeeklyCommand() using session.prompt()
2. `src/commands/1on1.ts` exists with execute1on1Command() using session.prompt()
3. `src/commands/incident.ts` exists with executeIncidentCommand() using SlackClient
4. `src/commands/brag.ts` exists with executeBragCommand() using GitHubClient
5. `src/commands/report.ts` exists with executeReportCommand() using session.prompt()
6. `src/commands/audit.ts` exists with executeAuditCommand() using SubAgentManager
7. `src/main.ts` wires all 6 commands to actual implementations
8. TypeScript compiles without errors
9. All Phase 3 commands registered in Obsidian command palette
</verification>

<success_criteria>
- /weekly command produces cross-session weekly summary with pattern discovery and 成果 report
- /1on1 command structures meeting notes into standard 1:1 format
- /incident command captures incident from Slack, reconstructs timeline, creates event doc
- /brag command records 成果 with evidence links to Brag Doc
- /report command generates performance review brief from evidence chain
- /audit command checks knowledge base: orphans, broken links, frontmatter gaps, stale content
- All commands registered in command palette and callable via slash prefix
</success_criteria>

<output>
After completion, create `.planning/phases/03-advanced/03-03-SUMMARY.md`
</output>
